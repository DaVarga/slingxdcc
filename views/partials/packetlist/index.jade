div(ng-controller="PacketListCtrl")
    .row
        .col-lg-12
            div(ng-class="{collapse:numPackets!=0}")
                .alert.alert-warning

                    button(type="button",ng-click="setPage(1)").btn.btn-default.pull-right
                        span.glyphicon.glyphicon-refresh

                    span
                        | Your search
                        b "{{searchString}}"
                        |did not match any packets.
                    p Suggenstions:
                        ul
                            li Make sure all words are spelled correctly.
                            li Try other keywords.



            div(ng-class="{collapse:numPackets==0}")

                button(type="button",ng-click="setPage(1)").btn.btn-default.pull-right
                    span.glyphicon.glyphicon-refresh

                h1(ng-class="{collapse:(searchString)}") List of all packets
                h1(ng-class="{collapse:(!searchString)}") Search for {{searchString}}



                .table-responsive
                    table(style="opacity:{{getOpacity()}}").table.table-hover.table-striped.table-condensed
                        thead
                            include tablehead
                        tfoot
                            include tablefoot
                        tbody
                            include column